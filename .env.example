# Copy this file to .env (which is gitignored) and fill in your key.
# Never commit .env. Inspect AI loads it automatically at runtime, so the
# together/ provider picks up TOGETHER_API_KEY without it touching your shell.
TOGETHER_API_KEY=
